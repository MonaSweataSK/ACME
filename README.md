# ACME

A starter Node.js project with Express.

## Scripts

- `npm start` – Runs the server.
- `npm run dev` – Starts the server with nodemon for hot-reloading.

## Development

Run `npm install` to install dependencies, then `npm run dev`.
